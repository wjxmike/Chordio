# Chord Hero UI 修改清单

对照设计稿 `Practice Screen1.svg` (402×874) 与当前小程序样式的差异，按执行顺序排列。完成一项可在前面打勾。

---

## 一、整体布局（解决「有时挤、有时空」）

- [x] **1.1 取消弹性留白，改为固定间距**
  - 去掉中间的 `spacer`（`flex: 1`）
  - 用固定 `margin` 控制：进度条 → 和弦块 → 选项区 → 底部按钮 的间距，与设计稿一致（设计稿为约 114rpx、114rpx、114rpx、140rpx 等）

- [x] **1.2 禁止页面滚动**
  - 确认 `page` 与 `.practice-container` 的 `overflow: hidden` 与 `height: 100vh` 生效，内容不超出不滚动

---

## 二、顶部和弦方块

- [x] **2.1 方块间距**
  - 将 `.chord-blocks-row` 的 `gap` 从 `16rpx` 改为 `22rpx`（设计稿约 11px → 22rpx）

- [x] **2.2 投影（可选）**
  - 为 `.chord-block` 增加与设计稿相近的 `box-shadow`（设计稿 dy≈4px，颜色 #535353 系）

---

## 三、答题选项 2×2

- [x] **3.1 卡片宽度**
  - 选项卡片宽度改为固定 `266rpx`（设计稿 133px×2），或通过 `grid-template-columns: 266rpx 266rpx` + 居中实现，避免 1fr 撑满导致过宽

- [x] **3.2 卡片间距**
  - 横向间距改为 `34rpx`，纵向改为 `50rpx`（设计稿约 17px、25px → 34rpx、50rpx）

- [x] **3.3 选项区与上方/下方的固定间距**
  - 选项区与和弦块之间：固定 `margin-top: 114rpx`（或与 1.1 统一）
  - 选项区与底部按钮之间：固定间距，由 1.1 统一

- [x] **3.4 投影（可选）**
  - 为 `.option-card` 增加与设计稿相近的 `box-shadow`（设计稿 dy≈5px）

---

## 四、底部按钮

- [x] **4.1 上下边距**
  - `margin-top` 从 `40rpx` 改为 `114rpx`（设计稿约 57px → 114rpx）
  - `margin-bottom` 从 `60rpx` 改为 `140rpx`（设计稿约 70px → 140rpx）

- [x] **4.2 投影（可选）**
  - 为 `.bottom-btn` 增加与设计稿相近的 `box-shadow`（设计稿 dy≈6px）

---

## 五、细节与统一

- [ ] **5.1 边框**
  - 设计稿描边为 2px，当前为 4rpx；若需严格一致可改为 2rpx 或保留 4rpx 以在 retina 上清晰（可讨论）

- [ ] **5.2 在真机/多机型上过一遍**
  - 确认无滚动、无裁切、间距在不同屏幕下仍协调

---

## 备注

- 设计稿尺寸按「px × 2 → rpx」换算（CLAUDE.md 约定）。
- 带「可选」的项可视效果需要再决定是否实施。
- 完成 1.1 + 3.1 + 3.2 + 4.1 即可明显缓解「挤」的问题。
